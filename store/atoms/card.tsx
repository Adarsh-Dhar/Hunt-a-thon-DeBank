import {atom} from "recoil"

export const priceAtom = atom({
    key: "priceAtom",
    default : 0
})

export const timeLeftAtom = atom({
    key: "timeLeftAtom",
    default : 0
})

export const finalAmountAtom = atom({
    key: "finalAmountAtom",
    default : 0
})

